const defaults_changeItem = ["Design", "Supplier / Sourcing", "Material", "Process / Method", "Machine", "Mold / Die", "Jig / Tooling", "Location"];

function genChangeItemCheckbox(defaults_changeItem, data) {
  let html = "";
  data.forEach((e) => {
    let item = e.item; //todo content
  });
}
